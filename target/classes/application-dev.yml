spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: false

# Disable OAuth2 resource server in dev – SecurityConfig uses HTTP Basic instead
logging:
  level:
    gov.fdic.tip: DEBUG
    org.springframework.security: DEBUG
